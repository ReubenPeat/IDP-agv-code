from machine import Pin
from utime import sleep

#Set the yellow LED pin and configuration
led_pin = 28
led = Pin(led_pin, Pin.OUT)

#Set the button pin
button_pin = 12
button = Pin(button_pin, Pin.IN, Pin.PULL_DOWN)

#Update the LED value and print said value
while not in start/finish area:
  led.value(button.value())
  flash using electronics
  
#Set the line senor pins
line_sensor_pin_1 = 12
line_sensor_pin_2 = 12
line_sensor_pin_3 = 12
line_sensor_pin_4 = 12
line_sensor_1 = Pin(button_pin, Pin.IN, Pin.PULL_DOWN)
line_sensor_2 = Pin(button_pin, Pin.IN, Pin.PULL_DOWN)
line_sensor_3 = Pin(button_pin, Pin.IN, Pin.PULL_DOWN)
line_sensor_4 = Pin(button_pin, Pin.IN, Pin.PULL_DOWN)

